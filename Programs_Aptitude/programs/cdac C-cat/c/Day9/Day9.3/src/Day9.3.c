/*
 ============================================================================
 Name        : 3.c
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
/*
	char name[8]="SUNBEAM"; // compiler adds /0 at end imp

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}

*/
/*
	char name[7]="SUNBEAM";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}
*/

/*
	char name[8]="SU\0BEAM";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}
*/
/*
	char name[8]="\0U\0BEAM";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}
*/
/*
	char name[]="SUNBEAM";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}

*/

/*
	char name[8]="";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}
*/
/*
	char name[]="";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}
*/
/*
	char name[]="A\0";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}
*/
	char name[8]="A";

	printf("\n name=%u",name);
	printf("\n size=%d strlen=%d\n",sizeof(name),strlen(name));

	int i=0;

	while(name[i]!=NULL)
	{
		printf("%c",name[i]);
		i++;
	}









	return 0;
}
